/**
 * The element and component styles webbies relies on are Drip's compliant
 * stylesheets (`@bearmetal/drip/ssr`'s `BMDripBase`, or `compliantCSS()`), so
 * they follow the loaded theme. Only the shared animation sheet lives here.
 *
 * @module
 */
export * from "./animations.ts";
