/**
 * Public types for `@bearmetal/webbies`.
 *
 * The components are classes, exported from `mod.ts`; there is no separate type
 * surface at the moment. The routing types that used to live here moved to
 * `@bearmetal/app` with the router built-ins.
 *
 * @module
 */

export {};
