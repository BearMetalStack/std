# cog

Internal flow-builder tooling reserved for the stack's setup wizard — a place for state-machine-style CLI flow primitives to live, distinct from the prompt/rendering primitives in `@bearmetal/cli`. Named `cog` (unscoped, not `@bearmetal/*`) so it stays invisible to the workspace's check/version/publish tooling; it is still early and mostly a scaffold today.
